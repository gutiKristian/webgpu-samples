import { Application } from "../../Application";
import { Renderer } from "../Renderer";

export class SimpleTriangle extends Renderer
{
    constructor(app: Application)
    {
        super(app);
    }

    render(): void {
        
    }
}